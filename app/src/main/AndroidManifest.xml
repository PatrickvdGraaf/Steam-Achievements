<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.crepetete.steamachievements">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SteamAchievementsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="n">

        <!-- Used by the AppAuth library to intercept authorisation responses. -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Accepts URIs that begin with "http://www.steamachievements.com/oauth2redirect” -->
                <data
                    android:host="com.crepetete.steamachievements"
                    android:path="/oauth2redirect"
                    android:scheme="http" />

                <data
                    android:host="steamcommunity.com"
                    android:path="/openid/id"
                    android:scheme="http" />
            </intent-filter>
        </activity>

        <!-- SPLASH-->
        <activity
            android:name=".ui.activity.splash.SplashScreenActivity"
            android:theme="@style/AppTheme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOGIN -->
        <activity android:name=".ui.activity.login.LoginActivity">
            <intent-filter>
                <action android:name="com.crepetete.steamachievements.HANDLE_AUTHORIZATION_RESPONSE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <!-- MAIN -->
        <activity android:name=".ui.activity.main.MainActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable_games" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>

        <!-- GAME DETAIL -->
        <activity
            android:name=".ui.activity.game.GameActivity"
            android:theme="@style/Transparent" />

        <!-- PAGER -->
        <activity
            android:name=".ui.activity.achievements.pager.TransparentPagerActivity"
            android:theme="@style/Transparent" />
    </application>

</manifest>